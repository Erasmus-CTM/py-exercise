---
title: "Small Python tasks, useful next steps"
format: html
filters: [_filters/example-source.lua, py-exercise]
ai-feedback:
  page-policy-files: feedback/python-policies.yml
  mode: copy
py-exercise:
  feedback: true
  submission: false
---

Complete the price calculation, then explore how different kinds of feedback
help with debugging and code review. Open the source below an activity to make
your own version.

**Feedback** prepares a message to paste into an AI chat. If your course has
feedback set up on the page, you can receive the reply here instead.


## Price including tax

Complete the return value so that the function includes the tax.

```{py-exercise}
#| label: price
#| task: Complete price_with_tax to return the price including tax. The rate is a decimal.
def price_with_tax(price, rate):
    tax = price * rate
    return price  # TODO: include tax

## TESTS ##
assert price_with_tax(100, 0.25) == 125
assert price_with_tax(80, 0) == 80
```

Choose Check to run the tests, then Feedback for guidance.

{{< include examples/_feedback-policies.qmd >}}
